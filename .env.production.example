# Production Environment Variables for VPS Deployment
# Copy this file to .env.production and fill in your values

# ============================================
# MongoDB Configuration (Docker Internal)
# ============================================
# IMPORTANT: Use 'mongodb' as hostname (Docker service name)
# Do NOT use localhost or 127.0.0.1
MONGODB_URI=mongodb://mongodb:27017
MONGODB_DATABASE=adventcalendar
MONGODB_PICTURES_COLLECTION=pictures
MONGODB_DUMMY_PICTURES_COLLECTION=dummy_pictures
MONGODB_USERS_COLLECTION=users

# ============================================
# JWT Secrets (REQUIRED - CHANGE THESE!)
# ============================================
# Generate secure random values with:
# openssl rand -base64 32
ACCESS_TOKEN_SECRET=CHANGE_ME_TO_RANDOM_32_CHARS
REFRESH_TOKEN_SECRET=CHANGE_ME_TO_RANDOM_32_CHARS

# ============================================
# Notes
# ============================================
# 1. NO MongoDB Atlas URI needed - using Docker MongoDB
# 2. These secrets are for JWT authentication tokens
# 3. Never commit .env.production to git
# 4. Use different secrets for dev vs production
